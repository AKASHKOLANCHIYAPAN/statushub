name: status_hub
description: "StatusHub – Quotes & Caption Generator for WhatsApp, Instagram & Social Media"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  google_mobile_ads: ^5.1.0
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  share_plus: ^7.2.1
  google_fonts: ^6.1.0
  flutter_staggered_animations: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

  assets:
    - assets/data/
    - assets/images/
